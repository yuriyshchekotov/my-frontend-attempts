{
  "name": "@frontend-learning/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for frontend-learning project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3"
  },
  "files": [
    "dist/**/*"
  ]
}

